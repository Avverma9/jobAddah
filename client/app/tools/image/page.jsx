import ImageEditorClient from "./ImageEditorClient";

export const metadata = {
  title: "Online Image Resizer & Passport Photo Maker | JobsAddah",
  description:
    "Free online tools to resize images, remove backgrounds, create passport size photos, and join images for sarkari exam forms.",
  keywords:
    "image resizer, passport photo maker, bg remover, image joiner, online image editor, jobsaddah image tool",
};

export default function ImageEditorPage() {
  return <ImageEditorClient />;
}
